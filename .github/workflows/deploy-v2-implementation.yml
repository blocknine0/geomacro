name: Deploy Updated AgentArena V2 Implementation

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Safety guard
        run: |
          if [ "${GITHUB_REF_NAME}" != "circle-grant-readiness" ]; then
            echo "Refusing deployment from ${GITHUB_REF_NAME}"
            echo "Select circle-grant-readiness in the Run workflow branch dropdown."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Build and test
        run: |
          forge build
          forge test

      - name: Deploy implementation only
        env:
          ARC_RPC_URL: ${{ secrets.ARC_RPC_URL }}
          DEPLOYER_PRIVATE_KEY: ${{ secrets.OWNER_PRIVATE_KEY }}
        run: |
          forge script script/DeployAgentArenaV2Implementation.s.sol:DeployAgentArenaV2Implementation \
            --rpc-url "$ARC_RPC_URL" \
            --broadcast \
            -vvvv
